const Router = require('express');

const router = new Router();

const { errorResponseTreatment } = require('../../../shared/utils/utils');

 router.post('/invitetechnician', async(req, res, next) => {
     try {
         
     } catch (error) {
         
     }
 } );

 module.exports = router;